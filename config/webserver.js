/**
 * The web server delivers the web pages
 * for the mobile web and desktop web UI.
 * Native mobile apps doesn't belong to this.
 * @type {Object}
 */
module.exports.WebServer = {
    port: 8083
};
